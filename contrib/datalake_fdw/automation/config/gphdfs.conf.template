paa_cluster:
    hdfs_namenode_host: ${HDFS_NAMENODE}
    hdfs_namenode_port: ${HDFS_PORT}
    hdfs_auth_method: simple
    hadoop_rpc_protection: authentication

hive_cluster:
    hdfs_namenode_host: ${HDFS_NAMENODE}
    hdfs_namenode_port: ${HDFS_PORT}
    hdfs_auth_method: simple
    hadoop_rpc_protection: authentication
