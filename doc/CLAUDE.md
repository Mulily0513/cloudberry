# doc/ — Documentation

SGML/XML-based documentation source for the Cloudberry/PostgreSQL manual.

## Build

```bash
make -C doc           # Build documentation (requires SGML/XML toolchain)
```

Source files in `doc/src/`. Output can be generated in multiple formats (HTML, man pages).
Online documentation: https://cloudberry.apache.org/docs/
